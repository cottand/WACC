# define and call a function with simple params

# Output:
# 13

# Program:

begin
  int f(int n, int m) is
    int i = 0;
    int j = 1;
    return i + j + n + m
  end
  int x = call f(10, 2) ;
  exit x
end
