stages:
        - compile
        - test
        - deploy


build:
        stage: compile
        script: ./gradlew build

check:
        stage: test
        script: ./gradle check
        artifacts:
                paths:
                        - build/reports/tests/test/


pages:
        stage: deploy
        dependencies:
                - job1
        script:
                - mv build/reports/tests/test/ public/
        artifacts:
                paths:
                        - public
                expire_in: 30 days
        only:
                - master

